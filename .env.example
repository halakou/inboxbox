# Cloudflare Worker secrets — set with wrangler, never commit values:
#   npx wrangler secret put BOT_TOKEN
#   npx wrangler secret put WEBHOOK_SECRET
#   npx wrangler secret put SETUP_KEY
#
# Optional local file (gitignored): worker/.dev.vars
# BOT_TOKEN=
# WEBHOOK_SECRET=
# SETUP_KEY=
